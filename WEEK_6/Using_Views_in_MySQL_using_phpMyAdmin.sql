-- PRACTICE PROBLEMS TO CREATE AND MANIPULATE A VIEW.

-- SELECT * FROM EMPLOYEES

/* CREATE VIEW
  EMP_DEPT AS
SELECT
  EMP_ID,
  F_NAME,
  L_NAME,
  DEP_ID
FROM
  EMPLOYEES; */
 
--SELECT * FROM DEPARTMENTS;
 
/* CREATE OR REPLACE VIEW EMP_DEPT AS
 SELECT 
 	E.EMP_ID,
  	E.F_NAME,
  	E.L_NAME,
  	D.DEPT_ID_DEP
 FROM
 	DEPARTMENTS D, EMPLOYEES E; */
 	
--SELECT * FROM EMP_DEPT;

-- DROP VIEW EMP_DEPT;

SELECT * FROM EMP_DEPT;

 	

 